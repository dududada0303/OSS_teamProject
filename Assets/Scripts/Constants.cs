using UnityEngine;

// 게임에 사용되는 상수를 나열하기 위한 정적 클래스
public static class Constants
{
    // 지면 초기화 위치    
    public const float GroundResetZPosition = -10.0f;

    // 지면 사이 Z 간격
    public const float GroundDistanceZ = 7.0f;
}
